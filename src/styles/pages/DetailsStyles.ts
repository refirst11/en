import css from 'styled-jsx/css';

const styles = css`
  .H1 {
    margin-top: 0;
    color: var(--color-pink);
  }

  .Time2 {
    font-size: 10px;
  }

  .color_link {
    color: var(--color-pink);
  }
`;

export default styles;
